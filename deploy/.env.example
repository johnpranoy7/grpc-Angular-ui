# Where users open the Angular app (must match CORS + environment.prod.ts)
UI_ORIGIN=http://grpc.johnpranoy7.dev,https://grpc.johnpranoy7.dev

# Change if host ports 80 or 8080 are already used
UI_HOST_PORT=80
BFF_HOST_PORT=8080
